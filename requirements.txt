# LSADRA V3 — Python Dependencies
# Core
pandas>=2.0.0
scikit-learn>=1.2.0
numpy>=1.24.0

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.51.0
pydantic>=2.0.0

# Dashboard
streamlit>=1.30.0
plotly>=5.14.0

# ML & Deep Learning
torch --index-url https://download.pytorch.org/whl/cpu
shap>=0.42.0
joblib>=1.3.0

# Explainability & Visualization
matplotlib>=3.7.0

# Auth & Security
python-jose[cryptography]>=3.3.0
# bcrypt used directly (passlib dropped — unmaintained + broke with bcrypt>=4; §6 #15)
bcrypt>=4.0.0

# HTTP client (agents, threat intel)
httpx>=0.24.0
requests>=2.31.0

# Logging
loguru>=0.7.0

# File watching (agent)
watchdog>=3.0.0

# Config
pyyaml>=6.0

# Background jobs
apscheduler>=3.10.0

# PDF reports
fpdf2>=2.7.0

# Image export for Plotly charts in reports
kaleido>=0.2.1

# Geolocation
geopy>=2.3.0

# Testing
pytest>=7.0.0
# Contract tests validate the demo corpus against docs/contracts/event-schema.v1.json
jsonschema>=4.0.0
